# Sparks — the shelf

Orphaned ideas waiting for the project they belong to. Born in one project, cut for good reasons,
kept because they might fit the next one. `research`'s directions move surfaces up to 3 per run —
unprompted, because sparks die if retrieval depends on remembering to look.

`wiki-lint` ages out sparks untouched for ~6 months (`status: aged-out` — never deleted, per ADR
semantics).

- [[Studio house language]] — a studio-default DESIGN.md as the greenfield fork base · born:
  design-studio · 2026-07
