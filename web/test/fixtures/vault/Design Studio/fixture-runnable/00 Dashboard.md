---
type: design-project
status: active
stage: build
client: Runnable Fixtures Co.
route:
started: 2026-03-01
prototype_repo:
---

# Fixture Runnable

A hermetic fixture whose prototype has a pre-authored `run` command (see
test/fixtures/prototypes.json). The canvas can start its dev server itself via
the Render control; once the server is ready the live frames mount. Used by
run-control.spec.ts — no external dev server required.

## Current stage

Current stage: build, awaiting your review (a legacy free-prose status line, not the colon grammar)

## Recommended next step

Render the prototype to see the running thing.

## Pipeline log

- Debrief — Ran, 2026-03-01
- Build — Current
