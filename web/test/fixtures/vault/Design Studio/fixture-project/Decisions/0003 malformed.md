---
id: 0003
status: [unterminated
tags: [decision
---

# 0003 — malformed frontmatter fixture

This file exists only to prove the vault reader skips a file with broken YAML frontmatter
instead of crashing the page. It must never appear in the rendered decision list.
